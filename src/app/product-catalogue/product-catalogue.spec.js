describe('ProductCatalogueCtrl', function() {

	describe('humanizeDate', function() {
		var ProductCatalogueCtrl;

		beforeEach(module('ascii-warehouse.product-catalogue'));

		beforeEach(inject(function($controller) {
			ProductCatalogueCtrl = $controller('ProductCatalogueCtrl');
		}));
	});
	
});
